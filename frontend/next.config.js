/** @type {import('next').NextConfig} */
const nextConfig = {
  // Backend API URL — override with NEXT_PUBLIC_API_URL env var in production
  // e.g. NEXT_PUBLIC_API_URL=https://api.yourapp.com npm run build
};

module.exports = nextConfig;
